# Atlas Agents — Requirements
# Install all dependencies: pip install -r requirements.txt

# ── Core (every chapter) ─────────────────────────────────────
openai>=1.60.0
pydantic>=2.0
python-dotenv>=1.0.0
PyYAML>=6.0

# ── Multi-provider (Ch. 3, 7) ────────────────────────────────
google-genai>=1.0.0
anthropic>=0.40.0
litellm>=1.50.0

# ── Frameworks (Part II) ─────────────────────────────────────
# Ch. 4 — Handoffs
openai-agents>=0.1.0

# Ch. 5 — State Graphs
langgraph>=0.3.0
langchain-openai>=0.3.0

# Ch. 6 — Multi-Agent
# crewai>=0.80.0

# Ch. 7 — Model Portability
dspy>=2.5.0

# ── Protocols (Ch. 8) ────────────────────────────────────────
mcp>=1.0.0

# ── Memory & RAG (Ch. 9) ─────────────────────────────────────
chromadb>=0.5.0
llama-index>=0.11.0

# ── Sandboxes (Ch. 10) ───────────────────────────────────────
e2b-code-interpreter>=1.0.0

# ── Guardrails (Ch. 12) ──────────────────────────────────────
nemoguardrails>=0.10.0

# ── Eval & Tracing (Ch. 13) ──────────────────────────────────
langsmith>=0.2.0

# ── Deployment (Ch. 14) ──────────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0
celery>=5.4.0
redis>=5.0.0
httpx>=0.27.0

# ── Optional: Local models (Ch. 7) ───────────────────────────
# Install Ollama separately: brew install ollama
# Install vLLM for GPU serving: pip install vllm
